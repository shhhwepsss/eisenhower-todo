import type { Zone } from '@/domain';

/**
 * Подпись зоны — единственная таблица на весь проект (docs/specs/35-design-system.md
 * §3, COLOR_NOT_ALONE), а не по одной в каждом месте, где видна задача. Названия
 * квадрантов уже показаны пользователю в матрице (`QUADRANT_META`/`INBOX_META`
 * читают `title` отсюда же) — заводить вторую копию значило бы разойтись по
 * смыслу, а не по стилю: было ровно так, «Минимизировать» в матрице и
 * «Делегировать» в списке для одного и того же Q3 (SSOT-регрессия, найдена на
 * визуальной проверке).
 *
 * У `inbox` подписи нет (см. `TaskRow`): «Входящие» — единственная зона без
 * цветности, называет её заголовок группы/зоны, и повторять его же текстом
 * под полосой, у которой и цвета-то нет, — дублирование без надобности.
 */
export const ZONE_LABELS: Record<Zone, string> = {
  Q1: 'Делать сейчас',
  Q2: 'Планировать',
  Q3: 'Минимизировать',
  Q4: 'Не делать',
  inbox: 'Входящие',
};
