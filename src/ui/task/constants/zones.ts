import type { Zone } from '@/domain';

/**
 * Подпись зоны — единственная таблица на весь проект (docs/specs/35-design-system.md
 * §3, COLOR_NOT_ALONE), а не по одной в каждом месте, где видна задача. Названия
 * квадрантов уже показаны пользователю в матрице (`QUADRANT_META`/`INBOX_META`
 * читают `title` отсюда же) — заводить вторую копию значило бы разойтись по
 * смыслу, а не по стилю: было ровно так, «Минимизировать» в матрице и
 * «Делегировать» в списке для одного и того же Q3 (SSOT-регрессия, найдена на
 * визуальной проверке).
 */
export const ZONE_LABELS: Record<Zone, string> = {
  Q1: 'Делать сейчас',
  Q2: 'Планировать',
  Q3: 'Минимизировать',
  Q4: 'Не делать',
  inbox: 'Входящие',
};

/**
 * Что написано на плашке строки, когда задача не разобрана. Не `ZONE_LABELS.inbox`:
 * строка лежит внутри карточки группы, заголовок которой и так называется
 * «Входящие», и повторять его под ним — дублирование, найденное на визуальной
 * проверке. Плашка при этом остаётся у всех строк: без неё заголовки соседних
 * строк начинались бы с разных мест.
 *
 * «Не разобрана» отвечает на другой вопрос, чем заголовок группы: не «где
 * лежит», а «в каком состоянии» — поэтому это отдельная подпись, а не копия.
 */
export const UNSORTED_ROW_LABEL: string = 'Не разобрана';
