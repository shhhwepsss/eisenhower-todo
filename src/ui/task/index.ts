// Публичный контракт слайса: карточка матрицы, строка списка и таблица подписей
// зон — SSOT для матрицы и списка (docs/specs/35-design-system.md §3).
// children/, constants/ и types/ — внутренности (SLICE_PUBLIC_API).
export { TaskCard } from './TaskCard';
export { TaskRow } from './TaskRow';
export { ZONE_LABELS } from './constants';
