/**
 * Состояние связи с хранилищем (docs/specs/4-architecture.md §5).
 *
 * Двух значений хватает: `error` — «прочитать или записать не удалось,
 * данные пользователя в опасности», всё остальное — `ready`. Что именно
 * пошло не так, знает `StorageError` в логе; экрану нужен только флаг.
 */
export type StorageStatus = 'ready' | 'error';
